
## Catalog tracking

This project is tracked in the personal project catalog: private repo `cgaravitoq/projects`, file `projects/shopify-agent.md`. Before ending a work session here, update that entry — `status`/`next`/`updated` frontmatter + one `## Log` line — and push the catalog (or use the projects-catalog MCP `update_project` tool).
